{
  "$schema": "https://srs.semanticops.com/schema/2.0/field.json",
  "id": "42566ce5-be6b-4c7e-97b2-22925cd83dba",
  "namespace": "com.semanticops.srs",
  "name": "vocabulary_ref",
  "version": 1,
  "description": "Reference to a named Vocabulary (namespace/name@version). Used on select/multiselect Fields as an alternative to inline selectOptions. Must resolve to a mode:closed Vocabulary (V3).",
  "valueType": "string",
  "createdAt": "2026-06-03T00:00:00Z",
  "aiGuidance": {
    "purpose": "Binds a select or multiselect field to a shared, installable Vocabulary rather than an inline string list. The referenced Vocabulary must be closed."
  }
}
