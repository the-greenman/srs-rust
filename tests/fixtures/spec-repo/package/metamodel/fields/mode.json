{
  "$schema": "https://srs.semanticops.com/schema/2.0/field.json",
  "aiGuidance": {
    "purpose": "inline = nested object(s); reference = target instance id(s). datatype == ref only, fixed per Field."
  },
  "createdAt": "2026-07-29T00:00:00Z",
  "description": "inline = nested object(s); reference = target instance id(s). datatype == ref only, fixed per Field.",
  "id": "4b000019-0000-4000-a000-000000000019",
  "name": "mode",
  "namespace": "com.semanticops.srs",
  "fieldType": {
    "datatype": "string",
    "valueDomain": "closed",
    "allowedValues": [
      "inline",
      "reference"
    ]
  },
  "version": 1
}
