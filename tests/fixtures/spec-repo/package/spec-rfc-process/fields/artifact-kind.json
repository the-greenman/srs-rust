{
  "$schema": "https://srs.semanticops.com/schema/2.0/field.json",
  "id": "5a000017-0000-4000-a000-000000000017",
  "namespace": "com.semanticops.spec",
  "name": "artifact-kind",
  "version": 1,
  "description": "The kind of artifact included in an RFC proposal.",
  "aiGuidance": {
    "purpose": "Classifies proposed artifacts so tooling can validate and review them without treating them as active spec state.",
    "extraction": "Choose the closest artifact kind. Use other only when none of the portable kinds apply."
  },
  "valueType": "select",
  "allowedValues": ["field", "type", "schema", "package", "view", "document", "script", "other"],
  "editorHint": "dropdown",
  "createdAt": "2026-05-27T00:00:00Z"
}
